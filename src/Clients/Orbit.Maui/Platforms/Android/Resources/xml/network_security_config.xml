<?xml version="1.0" encoding="utf-8"?>
<!-- Android refuses plaintext HTTP from API 28 on, which is what a development server on the Mac
     speaks. This is the narrow exception for that, and the counterpart of iOS's NSAllowsLocalNetworking
     in Platforms/iOS/Info.plist: the emulator's fixed alias for its host, and loopback for a device
     forwarding a port. Everything else keeps the refusal, so nothing on the public internet is reachable
     in the clear. Setting android:usesCleartextTraffic on the application would have done the job by
     removing the protection entirely. -->
<network-security-config>
	<domain-config cleartextTrafficPermitted="true">
		<domain includeSubdomains="false">10.0.2.2</domain>
		<domain includeSubdomains="false">localhost</domain>
		<domain includeSubdomains="false">127.0.0.1</domain>
	</domain-config>
</network-security-config>
