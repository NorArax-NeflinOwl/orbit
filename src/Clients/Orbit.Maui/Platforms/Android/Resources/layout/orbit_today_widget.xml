<?xml version="1.0" encoding="utf-8"?>
<!-- What the launcher draws. Only the handful of views RemoteViews can inflate are usable here, so this
     is deliberately flat: a heading, a container the lines are added into one at a time (see
     OrbitTodayWidget), and two lines of text for the cases where there are no lines at all. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/orbit_widget_background"
    android:padding="12dp">

    <TextView
        android:id="@+id/widgetDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/orbitWidgetSubtleText"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <LinearLayout
        android:id="@+id/widgetLines"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="4dp" />

    <TextView
        android:id="@+id/widgetMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="6dp"
        android:textColor="@color/orbitWidgetText"
        android:textSize="14sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widgetMore"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:textColor="@color/orbitWidgetSubtleText"
        android:textSize="11sp"
        android:visibility="gone" />
</LinearLayout>
