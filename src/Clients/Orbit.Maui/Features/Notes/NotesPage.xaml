<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="Orbit.Maui.Features.Notes.NotesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Orbit.Maui.Controls"
    xmlns:notes="clr-namespace:Orbit.Maui.Features.Notes"
    xmlns:screens="clr-namespace:Orbit.Mobile.Screens.Notes;assembly=Orbit.Mobile"
    x:DataType="screens:NotesViewModel"
    x:Name="Page"
    Title="{controls:Translate 'Notes'}">

    <ContentPage.Resources>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*,Auto">


    <!-- The screen at rest is what the design shows: a column of notes and nothing above them. The
         name is in the bar, the plus is the button over the foot of the list, and the field that plus
         unfolds is the only thing that ever sits between the two. -->
    <Grid Grid.Row="1" RowDefinitions="Auto,*,Auto" Padding="20,6,20,0" RowSpacing="10">

        <VerticalStackLayout Grid.Row="0" Spacing="10">
            <!-- The one thing this screen has to say for itself, which today is only about pinning.
                 It was set and never shown, so pinning with no connection did nothing and said
                 nothing - see NotesViewModel.HasMessage. -->
            <Label Text="{Binding Message}" IsVisible="{Binding HasMessage}"
                   LineBreakMode="WordWrap" Style="{StaticResource InfoLabel}" />

            <Grid x:Name="AddRow" IsVisible="False" ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <Entry x:Name="AddField" Grid.Column="0"
                       Placeholder="{controls:Translate 'New note'}"
                       Text="{Binding NewNoteTitle}"
                       ReturnType="Done"
                       ReturnCommand="{Binding AddNoteCommand}" />
                <Button Grid.Column="1" Text="{controls:Translate 'Add'}" Style="{StaticResource PrimaryButton}"
                        Command="{Binding AddNoteCommand}" />
            </Grid>
        </VerticalStackLayout>

        <RefreshView Grid.Row="1"
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding LoadCommand}">
            <CollectionView ItemsSource="{Binding Notes}"
                            SelectionMode="Single"
                            SelectionChangedCommand="{Binding OpenCommand}"
                            SelectionChangedCommandParameter="{Binding SelectedItem, Source={RelativeSource Self}, x:DataType=CollectionView}">

                <CollectionView.EmptyView>
                    <Label Text="{controls:Translate 'No notes.'}" Padding="0,12" Style="{StaticResource EmptyHint}" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="screens:NoteListItem">
                        <!-- The row the design draws, and nothing else on it:
                                 [pin] Shopping                              HIGH
                                 Flour, rice, olive oil
                                 yesterday
                             A row rather than a card - see ItemCard.IsBordered. A list of boxes reads as
                             a list of things kept apart; a column of rules reads as one list, which is
                             what a page of notes is.

                             No "⋯" here any more. Deleting a note, or taking somebody else's off your own
                             list, is under the note's own name once it is open - which is where the design
                             puts everything that can be done to a thing, and it took a control off every
                             row in the list to say so. -->
                        <controls:ItemCard IsBordered="False"
                                           Name="{Binding DisplayTitle}" IsPinned="{Binding IsPinned}">
                            <controls:ItemCard.Pin>
                                <!-- Filled when it is set, outlined when it is not, so the card says
                                     which without a word - and absent on a note somebody else owns,
                                     whose order is theirs. -->
                                <controls:PinButton IsVisible="{Binding CanBePinned}"
                                                    IsPinned="{Binding IsPinned}"
                                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=ViewModel.TogglePinCommand, x:DataType=notes:NotesPage}"
                                                    CommandParameter="{Binding .}" />
                            </controls:ItemCard.Pin>

                            <controls:ItemCard.Trailing>
                                <!-- The one tag the design puts at the end of the name's line. Only
                                     where it says something - see PriorityChoice.IsWorthSaying. -->
                                <Border Style="{StaticResource FilterChip}" Padding="7,2"
                                        IsVisible="{Binding HasPriority}">
                                    <Label Text="{Binding Priority}" Style="{StaticResource FilterChipLabel}" />
                                </Border>
                            </controls:ItemCard.Trailing>

                            <controls:ItemCard.Tags>
                                <!-- Two cards with the same title are otherwise indistinguishable, and
                                     the reader has no way of telling which one they wrote in. -->
                                <Label Text="{controls:Translate 'copy'}" IsVisible="{Binding IsCopy}"
                                       Style="{StaticResource RowMeta}" />
                            </controls:ItemCard.Tags>

                            <controls:ItemCard.Body>
                                <VerticalStackLayout Spacing="6">
                                    <!-- The note's first line, which is the only thing on the row that
                                         says what the note is about. One line and truncated: a preview
                                         that wraps turns a list into a page of paragraphs. -->
                                    <Label Text="{Binding Preview}" IsVisible="{Binding HasPreview}"
                                           FontSize="13" MaxLines="1" LineBreakMode="TailTruncation"
                                           Style="{StaticResource SubtleLabel}" />
                                    <!-- A hidden card keeps its place - a note vanishing from the list
                                         would look deleted - but says nothing about itself until
                                         unlocked. -->
                                    <Button Text="{controls:Translate 'Unlock'}" IsVisible="{Binding IsHidden}"
                                            FontSize="12" Padding="12,2" HorizontalOptions="Start"
                                            Style="{StaticResource LinkButton}"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=ViewModel.UnlockPrivateCommand, x:DataType=notes:NotesPage}" />
                                </VerticalStackLayout>
                            </controls:ItemCard.Body>

                            <controls:ItemCard.Extras>
                                <VerticalStackLayout Spacing="2">
                                    <Label Text="{Binding Updated}" IsVisible="{Binding CanBeOpened}"
                                           Style="{StaticResource ItemCardFooter}" />
                                    <!-- Why a note is waiting, or why it cannot be edited right now. -->
                                    <Label Text="{Binding Status}" IsVisible="{Binding HasStatus}"
                                           LineBreakMode="WordWrap" Style="{StaticResource ItemCardFooter}"
                                           TextColor="{DynamicResource Accent}" />
                                </VerticalStackLayout>
                            </controls:ItemCard.Extras>
                        </controls:ItemCard>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <!-- The rule under the last row. Without it the column stops mid-air, which reads as a
                     list that has been cut off rather than one that has ended. In the footer rather
                     than in a row of the page's grid, so that it follows the last note instead of
                     sitting at the bottom of the screen with a field of nothing above it. -->
                <CollectionView.Footer>
                    <BoxView Style="{StaticResource Hairline}" IsVisible="{Binding HasNotes}" />
                </CollectionView.Footer>

            </CollectionView>
        </RefreshView>


    </Grid>
        <!-- Across the foot, under everything the page itself draws - see AdBanner, and .ad-banner
             in Orbit.Web's app.css, which is the same bar on a narrow window. -->
        <controls:AdBanner Grid.Row="2" />
        <controls:Fab x:Name="AddButton" Grid.Row="1" IsAboveAdBar="True"
                      Description="{controls:Translate 'Add note'}" />
        <controls:NavigationBar Grid.Row="0" />
        <controls:MenuOverlay Grid.Row="0" Grid.RowSpan="3"
                              BindingContext="{Binding Menu, Source={x:Reference Page}, x:DataType=notes:NotesPage}" />
        <controls:AvatarMenu Grid.Row="0" Grid.RowSpan="3" />
        <controls:Drawer Grid.Row="0" Grid.RowSpan="3" />
    </Grid>

</ContentPage>
